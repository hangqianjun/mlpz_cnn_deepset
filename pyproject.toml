[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cnnpz"
version = "0.1.0"
description = "CNN photo-z ensemble utilities: data transforms, robust stats, plotting, and model training"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn>=1.0.1",
    "tensorflow>=2.8.0",
    "matplotlib",
    "h5py",
    "scipy",
    "packaging",
]

[project.optional-dependencies]
qp = ["qp-prob>=1.0.1"]

[tool.setuptools]
packages = ["cnnpz"]

[tool.ruff]
line-length = 120
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E741"]

[tool.ruff.lint.per-file-ignores]
"cnnpz/__init__.py" = ["F401", "F403", "E402"]
